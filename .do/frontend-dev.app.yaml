alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED
features:
  - buildpack-stack=ubuntu-22
ingress:
  rules:
    - component:
        name: react
      match:
        path:
          prefix: /
name: tutorpal-app-dev
region: sgp
static_sites:
  - build_command: bun run generate:api && bun run build --mode dev
    environment_slug: bun
    github:
      branch: main
      repo: psatid/tutorpal
    name: react
    source_dir: frontend
    catchall_document: index.html
